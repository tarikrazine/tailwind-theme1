import { helpers } from "../../../styles"

interface  NavProps {}

const Nav = (props: NavProps) => {
  return (
    <nav className={`${helpers.boxWidth}`}>
          Navbar
    </nav>
  )
}

export default Nav